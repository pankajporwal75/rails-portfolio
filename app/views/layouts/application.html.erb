<!DOCTYPE html>
<html>
  <head>
    <title>Portfolio | Pankaj Porwal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.9.0/css/all.min.css" %>
    <%= javascript_include_tag "application" %>
  </head>

  <body>
  <div class="container">
    <%= render 'shared/navbar' if user_signed_in? %>
    <div class="flash_messages">
      <%#= render 'shared/toaster_flash' %>
      <%= render 'shared/toastr_flash_new' %>
    </div>
    <div class="content-container container">
      <%= yield %>
    </div>
  </div>
  <%= render 'shared/modal' %>
  </body>
</html>
